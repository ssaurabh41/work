__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/
.venv/
venv/

# Exported output. The golden files are the regression suite's reference and
# must be committed -- without them it regenerates on a fresh clone and passes
# without checking anything.
*.svg
!examples/*.svg
!tests/golden/*.svg
